<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="NavDump"
        android:supportsRtl="true"
        android:theme="@style/Theme.NavDump">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          exported MUST be true: the system_server binds this from a different
          UID. It is protected by BIND_NOTIFICATION_LISTENER_SERVICE, which only
          the system holds, so nothing else can bind it.
        -->
        <service
            android:name=".NavDumpService"
            android:exported="true"
            android:label="NavDump"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>
